= form_for(@meetup) do |f|
  - if @meetup.errors.any?
    #error_explanation
      %h2
        = pluralize(@meetup.errors.count, "error")
        prohibited this meetup from being saved:
      %ul
        - @meetup.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :title
    %br/
    = f.text_field :title
  .field
    = f.label :description
    %br/
    = f.text_area :description
  .actions
    = f.submit
